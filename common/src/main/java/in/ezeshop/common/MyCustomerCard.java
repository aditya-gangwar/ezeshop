package in.ezeshop.common;

//import in.myecash.common.database.CustomerCardsNew;

/**
 * Created by adgangwa on 06-03-2017.
 */
/*public class MyCustomerCard {
    private CustomerCards custCard;
    private CustomerCardsNew custCardNew;

    public CustomerCards getCustomerCard() {
        if(custCard==null)
            convert();
        return custCard;
    }

    public void setCustCard(CustomerCards custCard) {
        this.custCard = custCard;
        this.custCardNew = null;
    }

    public void setCustCardNew(CustomerCardsNew custCardNew) {
        this.custCardNew = custCardNew;
        this.custCard = null;
    }

    private void convert() {
        custCard.setCard_id(custCardNew.getCard_id());
        custCard.setCcntId(custCardNew.getCcntId());
        custCard.setBarCode(custCardNew.getBarCode());
        custCard.setCustId(custCardNew.getCustId());
        custCard.setMchntId(custCardNew.getMchntId());
        custCard.setAgentId(custCardNew.getAgentId());
        custCard.setCardNum(custCardNew.getCardNum());
        custCard.setOrderId(custCardNew.getOrderId());
        custCard.setStatus(custCardNew.getStatus());
        custCard.setStatus_reason(custCardNew.getStatus_reason());
        custCard.setStatus_update_time(custCardNew.getStatus_update_time());
        custCardNew = null;
    }
}*/
